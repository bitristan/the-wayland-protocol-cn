# 深入 XDG shell

到目前为止，我们已经能够在屏幕上把一个应用显示在顶层窗口中了，但 XDG shell 还有更多内容尚未被我们充分了解。即便是最简单的应用，正确地实现配置（configuration）生命周期也会大有裨益，而且 xdg-shell 也为更复杂的应用提供了实用的特性。xdg-shell 的完整特性集涵盖：客户端与服务端就窗口尺寸进行的协商、多窗口层级结构、客户端侧装饰（client-side decorations），以及诸如上下文菜单这类窗口的语义化定位。
